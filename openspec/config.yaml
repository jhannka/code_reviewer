schema:spec-driven

context: |
  Tech stack: Python (Google ADK agent), Node.js (package.json scripts)
  Architecture: Agentic workflow with terminal-based CLI menu (menu.py)
  Testing: No test framework detected
  Style: Standard PEP 8, CommonJS Node dependencies

testing:
  strict_tdd: true
  capabilities_markdown: |
    ## Testing Capabilities
    
    **Strict TDD Mode**: enabled
    **Detected**: 2026-08-05T19:21:50-05:00
    
    ### Test Runner
    
    - Command: `PYTHONPATH=.:code_reviewer .venv/bin/pytest`
    - Framework: pytest
    
    ### Test Layers
    
    | Layer       | Available | Tool        |
    | ----------- | --------- | ----------- |
    | Unit        | ✅        | pytest      |
    | Integration | ❌        | —           |
    | E2E         | ❌        | —           |
    
    ### Coverage
    
    - Available: ❌
    - Command: `—`
    
    ### Quality Tools
    
    | Tool         | Available | Command        |
    | ------------ | --------- | -------------- |
    | Linter       | ❌        | —              |
    | Type checker | ❌        | —              |
    | Formatter    | ❌        | —              |

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    guidelines:
      - Follow existing code patterns
    tdd: true
  verify:
    test_command: "PYTHONPATH=.:code_reviewer .venv/bin/pytest"
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
